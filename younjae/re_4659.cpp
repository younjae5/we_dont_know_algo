#include <iostream>
#include <algorithm>
#include <set>
#include <string>
#include <vector>

using namespace std;

int main(){
    string word;

    vector<char> vow = {'a', 'e', 'i', 'o', 'u'}; //모음
    vector<char> con = {'b', 'c', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'm', 'n', 'p', 'q', 'r', 's', 
    't', 'v', 'w', 'x', 'y', 'z'}; //자음


    while(true){
        cin >> word;
        if(word == "end"){
            break;
        }

        bool fact = false;
        int temp = 0;

        for(int i = 0; i < word.length(); i++){
            if(vow[])
        }


    }
}